---
import { Image } from "astro:assets";
import bento1 from "../assets/bento1.webp";
import bento2 from "../assets/bento2.webp";
import bento3 from "../assets/bento3.webp";
import bento4 from "../assets/bento4.webp";
---

<section id="galeria" class="py-24 bg-slate-50">
  <div class="container mx-auto px-4">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-5xl font-serif text-slate-800 mb-4">
        Rincones de Paz
      </h2>
      <p class="text-slate-600">Un recorrido visual por nuestros espacios</p>
    </div>

    <div
      class="grid grid-cols-1 md:grid-cols-4 md:grid-rows-3 gap-4 h-[1200px] md:h-[800px]"
    >
      <!-- Hero variation / Main Item -->
      <div
        class="md:col-span-2 md:row-span-2 relative group overflow-hidden rounded-2xl cursor-pointer"
      >
        <Image
          src={bento2}
          alt="Vista Principal"
          class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
          loading="lazy"
          widths={[400, 600, 800]}
          sizes="(max-width: 768px) 100vw, 50vw"
        />
        <div
          class="absolute inset-0 bg-black/20 group-hover:bg-black/10 transition-colors"
        >
        </div>
        <div
          class="absolute bottom-4 left-4 text-white opacity-0 group-hover:opacity-100 transition-opacity duration-300"
        >
          <p class="font-medium">El Litoral</p>
        </div>
      </div>

      <!-- Detail 1 -->
      <div
        class="md:col-span-1 md:row-span-1 relative group overflow-hidden rounded-2xl cursor-pointer"
      >
        <Image
          src={bento3}
          alt="Interior Cabaña"
          class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
          loading="lazy"
          widths={[400, 600]}
          sizes="(max-width: 768px) 100vw, 25vw"
        />
        <div
          class="absolute inset-0 bg-black/20 group-hover:bg-black/10 transition-colors"
        >
        </div>
      </div>

      <!-- Detail 2 -->
      <div
        class="md:col-span-1 md:row-span-1 relative group overflow-hidden rounded-2xl cursor-pointer bg-emerald-800 flex items-center justify-center p-6 text-center"
      >
        <div class="text-white/80 group-hover:text-white transition-colors">
          <p class="font-serif italic text-xl">
            "El silencio es el sonido más fuerte de todos"
          </p>
        </div>
      </div>

      <!-- Garden Large -->
      <div
        class="md:col-span-2 md:row-span-1 relative group overflow-hidden rounded-2xl cursor-pointer"
      >
        <Image
          src={bento1}
          alt="Jardines"
          class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
          loading="lazy"
          widths={[400, 600, 800]}
          sizes="(max-width: 768px) 100vw, 50vw"
        />
        <div
          class="absolute inset-0 bg-black/20 group-hover:bg-black/10 transition-colors"
        >
        </div>
      </div>

      <!-- Info Box -->
      <div
        class="md:col-span-1 md:row-span-1 bg-slate-200 rounded-2xl flex flex-col items-center justify-center p-6 text-center hover:bg-slate-300 transition-colors cursor-pointer"
      >
        <h3 class="text-slate-800 font-bold text-lg mb-2">Ubicación</h3>
        <p class="text-sm text-slate-600">A pasos del mar</p>
      </div>

      <!-- Another Detail -->
      <div
        class="md:col-span-1 md:row-span-2 relative group overflow-hidden rounded-2xl cursor-pointer"
      >
        <Image
          src={bento4}
          alt="Atardecer"
          class="w-full h-full object-cover object-right transition-transform duration-700 group-hover:scale-110 filter hue-rotate-15"
          loading="lazy"
          widths={[400, 600]}
          sizes="(max-width: 768px) 100vw, 25vw"
        />
        <div
          class="absolute inset-0 bg-black/20 group-hover:bg-black/10 transition-colors"
        >
        </div>
      </div>

      <!-- Filler -->
      <div
        class="md:col-span-1 md:row-span-1 bg-sky-100 rounded-2xl flex items-center justify-center group overflow-hidden"
      >
        <div
          class="w-16 h-16 rounded-full bg-sky-200 group-hover:scale-125 transition-transform duration-500"
        >
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  // Section title animation only
  gsap.from("#galeria .text-center", {
    y: 50,
    opacity: 0,
    duration: 1,
    scrollTrigger: {
      trigger: "#galeria",
      start: "top 80%",
      toggleActions: "play none none reverse",
    },
  });

  // No grid animations to ensure all elements display correctly
</script>
